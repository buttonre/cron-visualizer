// Copy this file to config.js and fill in your values.
// config.js is gitignored — safe to put real values there.

export const API_URL   = "http://localhost:8765";   // SSH tunnel: ssh -N -L 8765:127.0.0.1:8765 user@host
export const API_TOKEN = "CHANGE_ME_BEFORE_DEPLOY"; // must match TOKEN in cron_api.py
